streamlit
boto3
pandas
plotly
python-dateutil
python-dotenv
# >=1.8 for output_config (structured outputs) + adaptive thinking;
# <2 so a major-version rebuild cannot silently change the request surface
anthropic>=1.8,<2
numpy
openpyxl
cryptography
# SSO & Authentication
msal>=1.24.0
requests>=2.28.0
PyJWT>=2.8.0
firebase-admin>=6.2.0